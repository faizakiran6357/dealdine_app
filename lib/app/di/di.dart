
import 'package:get_it/get_it.dart';


final sl = GetIt.instance;


Future<void> initDependencies() async {
// UI-only: register any controllers/services you might need to access from UI
// Example: navigation helper, simple state holders


// sl.registerLazySingleton<AuthController>(() => AuthController());


// For now we register placeholders to follow Triple-DDD pattern
// Domain layer stubs could be registered here when you add real use-cases
}